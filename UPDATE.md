# Обновление боковых фотокарточек

Изменены только:

- `assets/css/public/side-portraits.css`
- `assets/js/public/side-portraits.js`

Что исправлено:

- все 6 карточек образуют две зеркальные параллельные колонки;
- левый и правый ряд имеют одинаковые размеры и интервалы;
- 3D-наклон одинаков для всех карточек, поэтому они не разворачиваются в разные стороны;
- изображения показываются целиком в пропорции 3:4;
- фотография немного выходит за декоративную рамку;
- удалено обрезание содержимого бокового блока;
- нижние карточки подняты выше и больше не упираются в бегущую строку;
- подписи под изображениями не используются.

`index.html`, `admin.html`, `config.js` и Supabase менять не нужно.
