# Как безопасно загрузить модульное обновление

1. Скачайте резервную копию текущего `config.js` из GitHub.
2. Распакуйте архив.
3. Скопируйте текущий `config.js` в корень папки рядом с `index.html`.
4. В GitHub удалите старые `index.html` и `admin.html` либо замените их новыми.
5. Загрузите целиком папку `assets`.
6. Загрузите новые `index.html`, `admin.html` и `README.md`.
7. **Не заменяйте рабочий `config.js`.**
8. Папку `supabase` можно загрузить как резервную копию, но повторно запускать SQL не требуется.
9. Дождитесь зелёной галочки в `Actions`.
10. Откройте сайт и нажмите `Ctrl + F5`.

Если миграция `supabase/optional_author_comment.sql` ещё не запускалась, клиент сохраняет невидимый совместимый маркер вместо пустого комментария. Публичный сайт удаляет этот маркер и не показывает секцию комментария. После запуска миграции база также принимает обычную пустую строку.

После проверки следующим этапом лучше отдельно доработать `twitch-theme.css` и мобильную версию, не трогая Supabase-логику.

## Автоматическая синхронизация Steam

1. Создай длинный случайный секрет и добавь его в Secrets Edge Function под именем `CRON_SECRET`.
2. Повторно задеплой `supabase/functions/steam-game`.
3. Открой `supabase/scheduled_steam_sync.sql` и замени Project URL, anon key и тот же `CRON_SECRET`.
4. Выполни SQL один раз в Supabase SQL Editor.
5. Проверь появление задания `steam-game-auto-sync` в `cron.job`.

Задание запускается каждые 6 часов. Предстоящие игры и игры без даты обновляются не чаще раза в 6 часов, вышедшие — не чаще раза в неделю. За один запуск обрабатывается до 18 игр, по три параллельно. Значения кооператива с источником `manual_admin` не перезаписываются.

## Актуальные эфиры 141 GANG

1. Создай приложение в Twitch Developer Console и добавь его Client ID и Client Secret в Supabase Edge Function Secrets как `TWITCH_CLIENT_ID` и `TWITCH_CLIENT_SECRET`.
2. Задеплой публичную функцию: `supabase functions deploy stream-status --no-verify-jwt`.
3. Открой `141 GANG`: функция получает статус, название, категорию и время начала эфира с Twitch на сервере, не раскрывая Client Secret в браузере. Статусы обновляются каждые две минуты, а длительность эфира — каждые 30 секунд, пока панель открыта.
4. Kick-канал проверяется этой же функцией. Если платформа временно не отвечает, карточка показывает «Статус недоступен», а не ошибочный офлайн или LIVE.

Если `stream-status` ещё не развёрнута или Twitch Secrets не настроены, браузер сначала подтверждает LIVE через Twitch Player, а затем отдельно запрашивает публичные название, категорию и продолжительность эфира. Это резервный режим; серверная функция остаётся предпочтительным источником данных.

Для `yurapivo` и `r4dom1r`, чьи трансляции могут быть ограничены территорией РФ, резервная проверка метаданных выполняется до запуска Twitch Player. Поэтому региональная блокировка самого плеера не превращает активный эфир в «Статус недоступен».

## Управление каталогом на основном сайте

Кнопка «Управление» на `index.html` открывает встроенную панель. Для входа используются те же Supabase Auth email и пароль, что раньше использовались на `admin.html`; доступ по-прежнему дополнительно проверяется через `is_site_admin()`.

Старый адрес `admin.html` оставлен только как совместимый редирект на `index.html#admin`. Закладки и старые ссылки продолжат работать, но отдельная страница админ-панели больше не загружается.

## Пройденные, неинтересные и избранные игры

Перед публикацией frontend один раз выполни `supabase/game_library_status.sql` в Supabase SQL Editor. Миграция добавляет `library_status` и `is_favorite`; существующие игры останутся в основном каталоге. Изменять отметки может только администратор благодаря действующим RLS-политикам таблицы `games`.

## Авторизация зрителей через Twitch

1. Создай приложение в Twitch Developer Console.
2. Один раз выполни `supabase/twitch_viewer_auth.sql` в SQL Editor: он разрешает авторизованным зрителям читать опубликованные игры, но не даёт им административных прав.
3. В Supabase открой Authentication → Providers → Twitch и сохрани Client ID и Client Secret. Client Secret нельзя добавлять в GitHub или `config.js`.
4. В качестве OAuth callback в Twitch укажи callback URL, который показывает Supabase для Twitch Provider (`https://<project-ref>.supabase.co/auth/v1/callback`).
5. В Supabase Authentication → URL Configuration укажи Site URL `https://archivebtw.github.io/predlozhka-cr7/` и добавь этот же адрес в Redirect URLs.
6. После сохранения настроек кнопка «Войти через Twitch» запускает PKCE OAuth, возвращает пользователя на сайт, показывает Twitch-имя/аватар и позволяет выйти повторным нажатием.

Вход через Twitch не выдаёт права администратора: административные операции по-прежнему требуют наличие UUID пользователя в `public.site_admins` и успешный `is_site_admin()`.
